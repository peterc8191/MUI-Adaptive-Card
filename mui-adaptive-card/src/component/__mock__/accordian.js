import { mockAccordianGroup } from "./mockComponents";

export const accordian = {
  type: "AdaptiveJson",
  version: "1.0",
  body: [
    {
      type: "Card",
      items: [mockAccordianGroup],
    },
  ],
};
